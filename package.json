{
  "scripts": {
    "clean": "if exist dist rmdir /s /q dist",
    "build": "npm run clean && npx tsc --project tsconfig.json && tsc-alias -p tsconfig.json",
    "build-css": "sass --watch ./style/main.scss ./style/style.css"
  },
  "dependencies": {
    "sass": "^1.90.0"
  }
}
